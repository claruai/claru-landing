# Progress Log

Started: Thu Mar 12 16:00:00 EST 2026
Project: SEO / AEO / GEO Optimization
Branch: ralph/seo-aeo-geo

---

## Pre-Loop Completed
- [x] src/app/sitemap.ts created — dynamic sitemap with 82 URLs
- [x] Sitemap submitted to Google Search Console (pending — will error until deployed)
- [x] .env.local copied from main worktree
- [x] GSC MCP installed and configured
- [x] Full SEO/AEO/GEO audit completed
- [x] Supporting docs generated: requirements, design, plan in tasks/docs/

## Key Context
- Only 1 of 82 pages indexed (homepage only). All others "unknown to Google".
- Homepage SSR broken: `if (!mounted) return null` causes crawlers to see empty page
- robots.txt blocks GPTBot, ClaudeBot, PerplexityBot — kills GEO
- No Organization/WebSite JSON-LD at root level
- No llms.txt, no canonical URLs on ~10 pages, ~25 pages missing OG images
- Design system: bg #0a0908, accent #92B090, text #FFFFFF, fonts Geist Sans + JetBrains Mono
- Legal entity: Reka AI Inc. (d/b/a Claru AI)
- Pillar layouts have good JSON-LD patterns to follow (FAQPage, BreadcrumbList, Service)

## Completed Stories

### US-001: Create robots.ts — unblock AI crawlers, add Sitemap directive
- Created `src/app/robots.ts` using Next.js MetadataRoute.Robots convention
- Default `Allow: /` for all user agents with `/admin/`, `/portal/`, `/experiment/`, `/api/` disallowed
- Explicitly allowed AI search/citation crawlers: GPTBot, ClaudeBot, Claude-SearchBot, Claude-User, PerplexityBot, Applebot-Extended
- Blocked AI training crawlers: Google-Extended, CCBot, Bytespider, meta-externalagent
- Added `Sitemap: https://claru.ai/sitemap.xml` directive
- Build verified: `/robots.txt` renders as static route

### US-004: Add canonical URLs to pages missing them
- Added `alternates: { canonical: "/path" }` to metadata in 9 files:
  - `src/app/data-catalog/layout.tsx` → `/data-catalog`
  - `src/app/case-studies/layout.tsx` → `/case-studies`
  - `src/app/for-annotators/layout.tsx` → `/for-annotators`
  - `src/app/data/layout.tsx` → `/data`
  - `src/app/labeling/layout.tsx` → `/labeling`
  - `src/app/privacy/page.tsx` → `/privacy`
  - `src/app/terms/page.tsx` → `/terms`
  - `src/app/prohibited-use/page.tsx` → `/prohibited-use`
  - `src/app/job-applicant-privacy/page.tsx` → `/job-applicant-privacy`
- All use relative paths; `metadataBase` in root layout resolves to `https://claru.ai`
- Portal/admin/experiment pages intentionally excluded
- Build verified: no type errors, all pages compile

### US-003: Create llms.txt for AI engine discoverability
- Created `public/llms.txt` as a static Markdown file following the llms.txt spec
- Starts with site name (`# Claru`), canonical description, and company context (Reka AI Inc.)
- Maps all major sections with full URLs and descriptions:
  - Homepage, Data Catalog, Expert Labeling, Data, Jobs, For Annotators
  - 4 pillar landing pages (Acquire, Enrich, Prepare, Validate)
  - 12 sub-pillar pages across all 4 pillars
  - All 11 individual case studies with titles and descriptions
  - 4 legal pages (privacy, terms, prohibited-use, job-applicant-privacy)
- Includes Contact section with website, contact form, and email
- File will be served at `/llms.txt` from the `public/` directory

## Architecture Notes
- Homepage SSR: Islands pattern — server component (content) + client component (animations)
- OG images: single route handler /api/og using Next.js ImageResponse API
- robots.ts: Next.js MetadataRoute.Robots convention
- JSON-LD: @graph array in root layout combining Organization + WebSite
- Read tasks/docs/seo-aeo-geo-design.md for full architecture details

### US-002: Add Organization + WebSite JSON-LD to root layout
- Added `<script type="application/ld+json">` to `src/app/layout.tsx` body with `@graph` array
- Organization schema: name "Claru", legalName "Reka AI Inc.", url "https://claru.ai", logo (absolute URL to android-chrome-512x512.png), description, contactPoint with email team@claru.ai
- Organization includes sameAs: GitHub org, LinkedIn company page
- Organization includes knowsAbout with 10 AI/ML topics
- WebSite schema: name "Claru", url "https://claru.ai", publisher references Organization
- No SearchAction (site has no search functionality per requirements)
- TypeScript compilation verified — no type errors
- Follows same pattern as pillar layout JSON-LD (`dangerouslySetInnerHTML`)

### US-005: Add dateModified to all structured data schemas
- Created `src/lib/constants.ts` with `BUILD_DATE` constant (evaluated once at build time as "YYYY-MM-DD")
- Added optional `dateModified` field to `CaseStudy` interface in `src/types/case-study.ts`
- Updated Article JSON-LD in `src/app/case-studies/[slug]/page.tsx` — uses `cs.dateModified ?? cs.datePublished`
- Added `dateModified: BUILD_DATE` to Service JSON-LD in all 16 pillar/sub-pillar layout files:
  - 4 pillar layouts: acquire, enrich, prepare, validate
  - 12 sub-pillar layouts: egocentric-video, synthetic-data, data-licensing, expert-annotation, rlhf, video-annotation, deduplication, multimodal-alignment, quality-scoring, benchmark-curation, bias-detection, red-teaming
- Each pillar layout imports `BUILD_DATE` from `@/lib/constants`
- TypeScript compilation verified — no type errors

### US-006: Update pillar page headings to question-form for AEO
- Converted 3 H2 headings per sub-pillar page (36 total across 12 pages) to question form
- All 12 sub-pillar pages updated:
  - acquire/data-licensing, acquire/egocentric-video, acquire/synthetic-data
  - enrich/expert-annotation, enrich/rlhf, enrich/video-annotation
  - prepare/deduplication, prepare/multimodal-alignment, prepare/quality-scoring
  - validate/benchmark-curation, validate/bias-detection, validate/red-teaming
- Pattern applied: problem H2 -> "What Is...?", process H2 -> "How Does Claru's...?", use-case H2 -> "Where/When/What...?"
- Enhanced answer-first lead paragraphs for use-case H2s (7 pages) to provide self-contained answers
- Preserved all FAQ, Case Studies, CTA, and technical specs headings unchanged
- No H1 headings modified; no styling changes
- TypeScript compilation verified — no type errors

### US-009: Add FAQPage schema to data-catalog page
- Added FAQPage JSON-LD to `src/app/data-catalog/layout.tsx` with 5 Q&A pairs
- Questions cover: dataset offerings, custom dataset requests, quality assurance, differentiation from public datasets, sample previews
- Each answer is 40-80 words, self-contained, and factually grounded in actual page content
- Follows exact same pattern as `src/app/for-annotators/layout.tsx` (const + dangerouslySetInnerHTML)
- Layout wraps children in `<>` fragment with `<script type="application/ld+json">`
- TypeScript compilation verified — no type errors

### US-007: Homepage SSR refactor — server component with client children
- Removed `'use client'` from `src/app/page.tsx` — now a server component
- Removed `useState`/`useEffect` mounted guard that caused `if (!mounted) return null` (crawlers saw empty page)
- Removed all `dynamic(() => ..., { ssr: false })` imports for content-bearing sections
- All section components (Hero, ProblemAgitation, Origin, TwoPaths, ProofOfWork, Testimonials, FinalCTA) now imported directly — they SSR their full content (headings, text, links)
- Created `src/app/components/providers/ClientProviders.tsx` — wraps children with LenisProvider and HeroBackground without gating content rendering. Children always render; mounted guard only controls effects layer.
- Created `src/app/components/sections/SectionBridge.tsx` — extracted inline motion bridge component
- Created `src/app/components/sections/AnimatedLogoWrapper.tsx` — client wrapper for `dynamic(() => ..., { ssr: false })` since that API cannot be used in server components
- SSR verification: `curl localhost:3099` returns full HTML with:
  - `<h1>` "Purpose-built data for frontier video labs."
  - 5 `<h2>` section headings
  - All internal links (/data, /labeling, /case-studies, /data-catalog, individual case study links)
  - CTA anchor link to #contact
  - Full paragraph text for all sections
- Build succeeds with no errors
- No visual regression — all Framer Motion and GSAP animations preserved (client components hydrate on top of SSR'd HTML)

### US-012: Create entity consistency checklist and profile
- Created `src/lib/entity-profile.ts` exporting typed `entityProfile` constant with: name, legalName, description, tagline, url, email, github, linkedin
- Object is `as const` for literal type inference; exported `EntityProfile` type alias
- Created `tasks/docs/entity-consistency-checklist.md` with:
  - Canonical company description (full + short variant for character-limited fields)
  - Platform table: LinkedIn (exists), GitHub (exists), Crunchbase (needs creation), G2 (needs creation), Capterra (needs creation)
  - Each row includes expected URL, current status, which description variant to use, and sameAs inclusion
  - Current sameAs array documented with instructions to extend as new profiles are created
  - Sync instructions: single source of truth workflow, quarterly review process, new-platform checklist
- TypeScript compilation verified — no type errors

### US-010: Build dynamic OG image generator
- Created `src/app/api/og/route.tsx` using Next.js `ImageResponse` API with `runtime = "edge"`
- Accepts query params: `title` (required), `subtitle`/`description` (optional), `category` (optional)
- Six category variants: default, pillar, case-study, job, data-catalog, legal
- Category-specific styling: pillar gets `>` prompt prefix + "CAPABILITY" badge, case-study gets "CASE STUDY" badge, job gets "NOW HIRING" badge, legal gets muted #666 border
- Template design matches spec: #0a0908 background, #92B090 sage green accents, CLARU branding top-left, title in large white text, subtitle in muted gray, claru.ai URL with decorative lines bottom-left, category badge bottom-right, scanline overlay, left accent border
- Fonts loaded from Google Fonts CDN (JetBrains Mono + Geist Bold) with graceful fallback to system monospace/sans-serif
- Edge cases: missing title returns 400 JSON error, long titles truncated at 80 chars, long subtitles at 120 chars
- Output: 1200x630 PNG with Cache-Control headers (1 day client, 7 day CDN)
- Verified: GET /api/og?title=Test returns 200 image/png, GET /api/og returns 400
- TypeScript compilation verified — no type errors

### US-008: Add FAQPage schema to homepage
- Added FAQPage JSON-LD to `src/app/page.tsx` with 7 Q&A pairs targeting high-value search queries
- Questions cover: AI training data types, RLHF annotation quality, differentiation from general vendors, supported modalities, data privacy/security, human-in-the-loop definition, working with frontier AI labs
- Each answer is 40-80 words, self-contained, and factually grounded in Claru's actual services and capabilities
- Follows exact same pattern as `src/app/for-annotators/layout.tsx` and `src/app/data-catalog/layout.tsx` (const + dangerouslySetInnerHTML)
- JSON-LD `<script>` placed inside `<ClientProviders>` wrapper at top of JSX return
- TypeScript compilation verified — no type errors

### US-011: Wire OG images into all page metadata
- Created `src/lib/og.ts` with `ogImageUrl()` helper that generates `/api/og?title=...&category=...` URLs
- Updated root layout (`src/app/layout.tsx`) — replaced static `/og-image.png` with dynamic OG image URL in both openGraph.images and twitter.images
- Updated all 16 pillar/sub-pillar layouts with `images` array using `category: "pillar"`:
  - 4 pillar layouts: acquire, enrich, prepare, validate
  - 12 sub-pillar layouts: data-licensing, egocentric-video, synthetic-data, expert-annotation, rlhf, video-annotation, deduplication, multimodal-alignment, quality-scoring, benchmark-curation, bias-detection, red-teaming
- Updated `src/app/case-studies/[slug]/page.tsx` generateMetadata — OG image with case study title, `category: "case-study"`
- Updated `src/app/jobs/[slug]/page.tsx` generateMetadata — OG image with job title, `category: "job"`
- Updated 6 remaining page layouts with OG images:
  - `src/app/data-catalog/layout.tsx` — category: "data-catalog"
  - `src/app/for-annotators/layout.tsx` — with subtitle
  - `src/app/labeling/layout.tsx` — with subtitle
  - `src/app/data/layout.tsx` — with subtitle
  - `src/app/case-studies/layout.tsx` — category: "case-study" (listing page)
  - `src/app/jobs/layout.tsx` — category: "job" (listing page)
- Updated `jobs/[slug]/page.tsx` twitter card from `"summary"` to `"summary_large_image"` (only page that had plain "summary")
- TypeScript compilation verified — no type errors

## US-013: Playwright E2E — SEO Validation Suite

- Created `tests/e2e/seo-validation.spec.ts` with 11 tests using Page Object Model pattern
- Tests validate: homepage H1, Organization JSON-LD, canonical URL, FAQPage JSON-LD, robots.txt, sitemap.xml (50+ URLs), llms.txt, case-studies canonical, pillar FAQ schema, OG image endpoint, OG meta tag
- Added missing `alternates.canonical` to root layout metadata (`src/app/layout.tsx`)
- Used raw HTML parsing for JSON-LD extraction to handle client-component-rendered script tags reliably
- All 11 tests pass (5.6s runtime)

# Progress Log

Started: Mon Mar 23 15:08:00 EDT 2026
Project: Claru Landing
Goal: Unknown

---

## 2026-03-23 - US-001: Create clips table DDL on staging

**What I did:**
- Created `clips` table on staging DB (kpjcbemvwxffibiohmvi) via Supabase MCP
- 24 columns: core identity, annotation, technical, AI, derived, timestamps
- UNIQUE(s3_bucket, s3_key) constraint
- Auto-update trigger for updated_at
- HNSW index on embedding (vector_cosine_ops, m=16, ef_construction=64)
- GIN index on ann_metadata
- B-tree index on s3_bucket
- RLS enabled with service-role-only policy
- Created local migration file for reproducibility

**Files changed:**
- supabase/migrations/009_create_clips_table.sql (new)

**Learnings:**
- Supabase MCP apply_migration doesn't create local migration files — need to create manually
- vector extension v0.8.0 is available on staging
- Staging DB has 145k rows in video_index and 714 in dataset_samples (migration targets)

## 2026-03-23 - US-002: Create dataset_clips join table on staging

**What I did:**
- Created `dataset_clips` join table on staging via Supabase MCP
- 7 columns: id, dataset_id (FK datasets), clip_id (FK clips), lead_id (FK leads nullable), added_by, note, created_at
- Expression-based unique index: `(dataset_id, clip_id, COALESCE(lead_id, '00000000-...'))`
- CASCADE delete on dataset/clip FKs, SET NULL on lead FK
- RLS enabled with service-role-only policy

**Files changed:**
- supabase/migrations/010_create_dataset_clips_table.sql (new)

**Learnings:**
- Expression-based unique index works correctly with COALESCE for nullable columns

## 2026-03-23 - US-003: Create match_clips RPC on staging

**What I did:**
- Created `match_clips` RPC function (SECURITY DEFINER, search_path = public, extensions)
- Returns 15 fields from clips table + computed similarity
- LEFT JOINs dataset_clips only when filter_dataset_id is provided
- GROUP BY c.id to deduplicate when joining
- Sets hnsw.ef_search = 100 for quality
- Verified with zero-vector test query (0 rows, no errors)

**Files changed:**
- supabase/migrations/011_create_match_clips_rpc.sql (new)

**Learnings:**
- Casting `::vector(768)` must wrap the full concatenated string, not just the suffix
- LEFT JOIN + conditional WHERE is the pattern for optional dataset filtering

## 2026-03-23 - US-004: Migrate video_index to clips on staging

**What I did:**
- Created `scripts/migrate-video-index-to-clips.py`
- Maps: s3_bucket, s3_key, caption_text → ai_caption + caption_text, enrichment_source → ai_enrichment_source, embedding, indexed_at → created_at
- Sets caption_rebuilt_at for rows with valid embeddings
- INSERT ON CONFLICT DO NOTHING via PostgREST `resolution=ignore-duplicates`
- Batch 500 rows with 0.1s throttle, idempotent
- Supports --dry-run and --limit flags

**Files changed:**
- scripts/migrate-video-index-to-clips.py (new)

**Learnings:**
- PostgREST doesn't support SQL functions like `now()` as values — use Python datetime ISO format instead
- PostgREST range pagination: `Range: offset-end` header with `Prefer: count=exact`

## 2026-03-23 - US-005: Migrate dataset_samples to clips + dataset_clips

**What I did:**
- Created `scripts/migrate-dataset-samples-to-clips.py`
- Fetches 714 rows, skips 20 with NULL s3_object_key (logs them)
- Deduplicates by (s3_bucket, s3_object_key): 588 unique clips → one clips row each
- Re-embeds all caption text at 768 dims via OpenAI (existing were 1536-dim)
- Upserts clips with ON CONFLICT DO NOTHING
- Looks up clip IDs then inserts dataset_clips preserving dataset_id + lead_id
- Idempotent, supports --dry-run and --limit

**Files changed:**
- scripts/migrate-dataset-samples-to-clips.py (new)

**Learnings:**
- All 83 existing embeddings in dataset_samples are 1536-dim — ALL need re-embedding at 768
- 588 distinct s3_object_keys from 694 valid rows = ~15% duplication (same clip in multiple datasets)
- Caption text built from agent_context.caption + enrichment_json.description/visual_description


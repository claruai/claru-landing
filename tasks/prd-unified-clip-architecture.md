# PRD: Unified Clip Architecture

## Introduction

Replace the current two-table clip storage (`dataset_samples` + `video_index`) with a single `clips` table and a `dataset_clips` join table. Datasets become filtered views — "adding a clip to a catalog" is a single join table insert with no data copying. All surfaces (admin search, portal, MCP, loader scripts) render from one source of truth. Structured columns per data source (annotation, technical, AI enrichment) prevent enrichment passes from overwriting each other.

**Target: staging DB first (`kpjcbemvwxffibiohmvi`), validate, then replicate on prod (`usmgbihcevnvrkyvrlju`).**

## Goals

- Eliminate data duplication between dataset_samples and video_index
- Make "add clip to catalog" a single join table insert (no metadata copying)
- Prevent enrichment passes from overwriting each other (structured columns per source)
- Unify admin search into one mode (remove Catalog/Full Corpus/Both toggle)
- Portal and admin render clip data from the same source of truth
- Surface technical metadata (resolution, fps, duration, codec) as first-class queryable fields
- Clip detail view shows combined tabs: Annotation | AI Enrichment | Technical

## Supporting Documentation

The following docs were generated before this PRD and are referenced by user stories. **The implementing agent MUST read the referenced sections before starting each story.**

| Document | Generated By | Contents |
|----------|-------------|----------|
| `tasks/docs/unified-clip-architecture-requirements.md` | `kiro-requirement` | 14 requirements with acceptance criteria, edge cases, validation rules |
| `tasks/docs/unified-clip-architecture-design.md` | `kiro-design` | Schema DDL, migration strategy, API contracts, component hierarchy, RPC functions |
| `tasks/docs/unified-clip-architecture-plan.md` | `kiro-plan` | 12 phases, ~45 tasks, dependencies, parallelization, risks |

## User Stories

---

### US-001: Create `clips` table and `dataset_clips` join table on staging

**Description:** As a developer, I need the new schema created on the staging database so all subsequent work has a foundation.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: clips Table Schema, dataset_clips Join Table, Indexes)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 1, Requirement 2)

**Acceptance Criteria:**
- [ ] Create `clips` table with: id (uuid PK), s3_bucket, s3_key (UNIQUE pair), mime_type, filename
- [ ] Add annotation columns: ann_metadata (jsonb), ann_annotation_key (text), ann_specs_key (text)
- [ ] Add technical columns: tech_file_size_bytes (bigint), tech_duration_seconds (numeric), tech_resolution_width (int), tech_resolution_height (int), tech_fps (numeric), tech_codec (text), tech_bit_depth (int)
- [ ] Add AI columns: ai_caption (text), ai_agent_context (jsonb), ai_enrichment_source (text), ai_enrichment_json (jsonb)
- [ ] Add embedding column: vector(768), plus HNSW index (not IVFFlat — HNSW works on empty tables and handles incremental inserts)
- [ ] Add timestamps: created_at, updated_at with auto-update trigger
- [ ] Create `dataset_clips` table: id, dataset_id (FK), clip_id (FK), lead_id (FK nullable), added_by, note, created_at, UNIQUE(dataset_id, clip_id, COALESCE(lead_id, '00000000-...'))
- [ ] Create `match_clips` RPC function replacing both match_samples and match_video_index
- [ ] Enable RLS on both tables with appropriate policies
- [ ] Apply migration to staging DB via Supabase MCP
- [ ] Verify tables, indexes, and RPC exist on staging
- [ ] **Unit Tests:** Verify match_clips returns results for a test embedding, verify UNIQUE constraints prevent duplicates
- [ ] **Code Review:** Run code-reviewer agent on migration SQL

**Recommended agents/skills:** `supabase-expert`, `code-reviewer`

---

### US-002: Migrate video_index data → clips on staging

**Description:** As a developer, I need all 47k staging video_index rows migrated into the clips table so search works against the new schema.

**Reference docs:**
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 1, Task 1.4)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 3)

**Acceptance Criteria:**
- [ ] Create migration script `scripts/migrate-video-index-to-clips.py`
- [ ] Map: s3_bucket, s3_key, caption_text → ai_caption, enrichment_source → ai_enrichment_source, embedding → embedding, indexed_at → created_at
- [ ] Also copy caption_text → caption_text (derived field, already valid for video_index rows)
- [ ] Set `caption_rebuilt_at = NOW()` for rows with valid embeddings (prevents rebuild script from reprocessing 145k rows unnecessarily)
- [ ] Use INSERT ON CONFLICT DO NOTHING (no bloat)
- [ ] Process in batches of 500 with 0.1s throttle
- [ ] Validate: clips row count matches video_index count on staging
- [ ] Validate: match_clips RPC returns results after migration
- [ ] Script is idempotent (safe to re-run)
- [ ] **Unit Tests:** Verify row count match, verify no duplicate s3_key entries
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `python-backend-expert`, `code-reviewer`

---

### US-003: Migrate dataset_samples → clips + dataset_clips on staging

**Description:** As a developer, I need all 714 dataset_samples merged into clips (with re-embedding at 768-dim) and their dataset relationships preserved in dataset_clips.

**Reference docs:**
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 1, Task 1.5)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 3, Requirement 4)
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: Data Migration Strategy)

**Acceptance Criteria:**
- [ ] Create migration script `scripts/migrate-dataset-samples-to-clips.py`
- [ ] For each dataset_sample: upsert into clips matching on s3_bucket + s3_object_key
- [ ] Map: enrichment_json → ai_enrichment_json, agent_context → ai_agent_context, metadata_json → ann_metadata, s3_annotation_key → ann_annotation_key, s3_specs_key → ann_specs_key, file_size_bytes → tech_file_size_bytes, duration_seconds → tech_duration_seconds, resolution_width → tech_resolution_width, resolution_height → tech_resolution_height, fps → tech_fps
- [ ] Re-embed agent_context.scene_summary at 768 dims for clips that had 1536-dim embeddings
- [ ] Create dataset_clips rows preserving dataset_id + lead_id relationships
- [ ] Handle samples without s3_object_key gracefully (log and skip)
- [ ] Validate: dataset_clips count matches original dataset_samples count
- [ ] Script is idempotent
- [ ] **Unit Tests:** Verify dataset relationships preserved, verify lead-specific clips have lead_id in dataset_clips
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `python-backend-expert`, `code-reviewer`

---

### US-004: Update TypeScript types

**Description:** As a developer, I need TypeScript types that match the new schema so all app code has proper type safety.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: TypeScript Types)
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 2)

**Acceptance Criteria:**
- [ ] Add `Clip` interface matching clips table canonical schema (all ann_*, tech_*, ai_* columns)
- [ ] Add `DatasetClip` interface matching dataset_clips schema
- [ ] Add `ClipSearchResult` interface (unified, no more catalog vs full_corpus discrimination)
- [ ] Add field mapping table as code comments: `s3_object_key → s3_key`, `metadata_json → ann_metadata`, `agent_context → ai_agent_context`, `enrichment_json → ai_enrichment_json`, `duration_seconds → tech_duration_seconds`, etc.
- [ ] Mark `DatasetSample`, `VideoIndexRecord`, `LeadCustomSample` as `@deprecated`
- [ ] Remove `CatalogSearchResult` / `FullCorpusSearchResult` / `UnifiedSearchResult` discriminated union
- [ ] **DO NOT change `generateEmbedding()` default to 768 yet** — this must happen AFTER US-005 and US-010 migrate all call sites to match_clips. Instead, add explicit `768` parameter to all new call sites.
- [ ] Typecheck passes with no errors
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `frontend-expert`, `code-reviewer`

---

### US-005: Rewrite admin search API to use clips table

**Description:** As a developer, I need the search API to query the single clips table instead of querying dataset_samples and video_index separately.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: Search API Contract, match_clips RPC)
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 3)

**Acceptance Criteria:**
- [ ] Rewrite `src/app/api/admin/catalog/search/route.ts` to use match_clips RPC only
- [ ] Remove the 3-mode logic (catalog/full_corpus/both) — one code path
- [ ] Browse mode queries clips joined through dataset_clips when dataset filter is set
- [ ] Search mode calls match_clips with optional dataset filter (via dataset_clips join)
- [ ] Return ClipSearchResult shape (unified, no more source discrimination)
- [ ] Include structured fields in response: ann_metadata, ai_agent_context, tech_* fields
- [ ] Subcategory filtering uses ann_metadata fields instead of caption_text ILIKE
- [ ] Lead assignment lookup: collapse current two-query pattern (one for catalog IDs, one for source_video_index_id) into single `dataset_clips WHERE clip_id IN (...) AND lead_id IS NOT NULL` query
- [ ] Typecheck passes
- [ ] **Unit Tests:** Test search with/without dataset filter, test browse mode pagination, test empty results
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `nextjs-expert`, `code-reviewer`

---

### US-006: Rewrite admin search UI — remove mode toggle, unified results

**Description:** As an admin, I want one search experience across all clips without switching between "Catalog Samples" and "Full Corpus" modes.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: Admin Search UI)
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 4)

**Acceptance Criteria:**
- [ ] Remove the Catalog Samples / Full Corpus / Both mode toggle from CatalogSearchClient.tsx
- [ ] Update search page server component to derive bucket list and clip count from `clips` table (currently from `video_index` — will crash after drop)
- [ ] Single search input + dataset filter + subcategory filter
- [ ] Add resolution filter dropdown (All / 720p+ / 1080p+ / 4K)
- [ ] Add "Has AI description" toggle filter
- [ ] ResultCard renders unified ClipSearchResult (no more source-based discrimination)
- [ ] ResultCard shows: thumbnail, ai_caption excerpt, category/activity tags from ann_metadata, resolution badge from tech_*, similarity %
- [ ] ResultDetailModal shows structured tabs or sections instead of flat field list
- [ ] Remove all `source: "catalog" | "full_corpus"` logic
- [ ] Update AddToLeadButton: replace `videoIndexId`/`datasetSampleId` props with single `clipId` prop
- [ ] Typecheck passes
- [ ] Verify in browser using Playwright MCP
- [ ] **Unit Tests:** Test filter state management, test result card rendering with all field combinations
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `frontend-expert`, `code-reviewer`

---

### US-007: Rewrite bulk action bar — simplified clip-to-catalog flow

**Description:** As an admin, I want to add selected clips to any dataset with a single action, without the complexity of data copying or separate "create catalog" vs "add to catalog" flows.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: Admin Bulk Actions)

**Acceptance Criteria:**
- [ ] Simplify bulk action bar to two tabs: "Add to Dataset" and "Create New Dataset"
- [ ] "Add to Dataset" — pick existing dataset + optional lead, inserts dataset_clips rows
- [ ] "Create New Dataset" — name + lead, creates dataset then inserts dataset_clips rows
- [ ] Remove all data-copy logic (no more reading from video_index and writing to dataset_samples)
- [ ] Rewrite `POST /api/admin/catalog/custom` to use dataset_clips inserts only
- [ ] Update AddToLeadButton to insert dataset_clips instead of creating dataset_samples
- [ ] Typecheck passes
- [ ] Verify in browser using Playwright MCP
- [ ] **Unit Tests:** Test dataset_clips creation, test lead-specific assignment
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `frontend-expert`, `nextjs-expert`, `code-reviewer`

---

### US-008: Rewrite portal dataset detail page to use clips + dataset_clips

**Description:** As a portal lead, I want to see clip data from the unified clips table when browsing a dataset, with a detail view showing annotation, AI enrichment, and technical metadata tabs.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: Portal Detail Page, Clip Detail Tabs)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 8)

**Acceptance Criteria:**
- [ ] Portal dataset detail page queries clips via dataset_clips join instead of dataset_samples
- [ ] Field rename: `s3_object_key` → `s3_key`, `duration_seconds` → `tech_duration_seconds`, `metadata_json` → `ann_metadata`
- [ ] Remove `media_url` and `storage_path` fallback chain in URL resolution — all clips use `s3_bucket` + `s3_key` for presigning
- [ ] Lead filtering: show clips where dataset_clips.lead_id IS NULL OR = current lead, **deduplicate by clip_id** (use `SELECT DISTINCT ON (clip_id)`) to prevent duplicates when clip has both base + lead-specific entries
- [ ] SampleGallery receives clips data (with signed URLs from s3_bucket + s3_key)
- [ ] Update GalleryCard: `sample.metadata_json` → `clip.ann_metadata`, `sample.duration_seconds` → `clip.tech_duration_seconds`
- [ ] Clip detail modal shows structured tabs: Annotation | AI Enrichment | Technical
- [ ] Annotation tab: live-fetches annotation-data.json from S3 via `ann_annotation_key` (existing pattern), renders category/subcategory/activities
- [ ] AI Enrichment tab: shows ai_caption, ai_agent_context (scene_summary, environments, activities, objects)
- [ ] Technical tab: shows resolution, fps, duration, codec, file size, aspect ratio
- [ ] Hidden from client: ai_enrichment_source, project_tag, project_type, internal IDs
- [ ] Admin preview mode still works (admin-token cookie bypass)
- [ ] Deep-link ?sample= param still works (now ?clip= pointing to clip ID)
- [ ] Typecheck passes
- [ ] Verify in browser using Playwright MCP
- [ ] **Unit Tests:** Test lead filtering logic, test tab rendering with missing data
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `frontend-expert`, `nextjs-expert`, `code-reviewer`

---

### US-009: Rewrite portal catalog index page to use dataset_clips

**Description:** As a portal lead, I want the catalog browser to show datasets based on dataset_clips membership instead of dataset_samples.

**Acceptance Criteria:**
- [ ] Portal catalog page queries datasets via lead_dataset_access (unchanged)
- [ ] Sample counts derived from dataset_clips instead of dataset_samples
- [ ] **Rewrite `lead_custom_samples` query** on catalog list page → use `dataset_clips WHERE lead_id = current_lead` to show curated samples section (currently queries `lead_custom_samples` table which will be dropped)
- [ ] Curated samples section enriches from `clips` table instead of `video_index`
- [ ] Admin preview mode shows all published datasets (unchanged behavior)
- [ ] Typecheck passes
- [ ] Verify in browser
- [ ] **Unit Tests:** Test dataset count derivation, test curated samples rendering
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `nextjs-expert`, `code-reviewer`

---

### US-010: Update MCP server — search tools

**Description:** As an MCP agent, I need search_catalog and search_full_catalog merged into a single search_clips tool that queries the unified clips table.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: MCP Tool Updates)
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 8)

**Acceptance Criteria:**
- [ ] Replace `search_catalog` + `search_full_catalog` with single `search_clips` tool
- [ ] search_clips accepts: query, limit, dataset_id (optional), s3_bucket (optional), match_threshold
- [ ] Returns unified ClipSearchResult with structured fields (ann_metadata, ai_caption, tech_*)
- [ ] Update `build_lead_brief` to use search_clips — **NOTE:** currently makes TWO separate embedding calls (1536-dim for match_samples + 768-dim for match_video_index) and TWO RPC calls. Collapse into ONE 768-dim embedding + ONE match_clips call
- [ ] Update `get_dataset_overview` to query clips via dataset_clips
- [ ] Update `download_clips` to query clips table only
- [ ] Update `get_corpus_stats` to aggregate clips table
- [ ] Typecheck passes
- [ ] Test via curl against local MCP endpoint
- [ ] **Unit Tests:** Test each updated tool returns valid responses
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `nextjs-expert`, `code-reviewer`

---

### US-011: Update MCP server — catalog management tools

**Description:** As an MCP agent, I need catalog management tools to use dataset_clips inserts instead of data copying.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: MCP Tool Updates)

**Acceptance Criteria:**
- [ ] Rewrite `create_custom_catalog` — creates dataset + inserts dataset_clips rows (no data copy)
- [ ] Rewrite `add_clips_to_catalog` — inserts dataset_clips rows only
- [ ] Rewrite `remove_clips_from_catalog` — deletes dataset_clips rows (not clips themselves)
- [ ] Update `list_lead_catalogs` to query through dataset_clips
- [ ] Update `grant_lead_access` and `revoke_lead_access` (unchanged, but verify)
- [ ] Update `update_dataset` to derive total_samples from dataset_clips count
- [ ] Typecheck passes
- [ ] Test via curl against local MCP endpoint
- [ ] **Unit Tests:** Test create_custom_catalog creates dataset_clips not dataset_samples, test remove doesn't delete clips
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `nextjs-expert`, `code-reviewer`

---

### US-012: Update loader scripts to write to clips table

**Description:** As a developer, I need all enrichment/loader scripts to write to the clips table instead of video_index, using structured columns per data source.

**Reference docs:**
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 9)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 10)

**Acceptance Criteria:**
- [ ] Update `load-annotation-metadata.py` — writes ann_metadata, ann_annotation_key, tech_* columns on clips
- [ ] Update `load_cobry_captions.py` — writes ai_caption, ai_enrichment_source='cobry_caption' on clips
- [ ] Update `re-enrich-annotation-frames.py` — writes ai_caption, ai_enrichment_source='gemini_frame' on clips
- [ ] Each script writes ONLY to its own column group (no overwriting other sources)
- [ ] Each script sets caption_rebuilt_at = NULL to flag rows needing embedding rebuild
- [ ] All scripts use INSERT ON CONFLICT DO NOTHING (bloat prevention)
- [ ] Separate rebuild script computes derived caption_text + embedding from all sources
- [ ] **Unit Tests:** Verify each loader writes only to its columns, verify no cross-contamination
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `python-backend-expert`, `code-reviewer`

---

### US-013: Create caption rebuild script

**Description:** As a developer, I need a script that combines all source columns into caption_text and generates the 768-dim embedding, so search works correctly after any enrichment pass.

**Reference docs:**
- Design: `tasks/docs/unified-clip-architecture-design.md` (sections: Caption Rebuild Function)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 5)

**Acceptance Criteria:**
- [ ] Create `scripts/rebuild-clip-captions.py`
- [ ] Selects clips where embedding needs rebuild (caption_rebuilt_at IS NULL or new data added)
- [ ] Builds caption_text by combining: ann_metadata (category, subcategory, activities) + tech specs (resolution, fps, duration) + ai_caption (scene description)
- [ ] Generates 768-dim embedding via OpenAI text-embedding-3-small
- [ ] Updates caption_text, embedding, caption_rebuilt_at on each clip
- [ ] Processes in batches of 100, INSERT ON CONFLICT DO NOTHING for embedding updates
- [ ] Idempotent — safe to re-run
- [ ] **Unit Tests:** Test caption assembly from various field combinations (some null, all present, all null)
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `python-backend-expert`, `code-reviewer`

---

### US-014: Update admin catalog detail pages

**Description:** As an admin, I want the catalog detail page (samples list, edit panel) to read from clips via dataset_clips instead of dataset_samples.

**Acceptance Criteria:**
- [ ] Update `src/app/admin/catalog/[id]/page.tsx` to query clips via dataset_clips
- [ ] Update SamplesList to render clip data
- [ ] Update SampleEditPanel to edit clip metadata (ann_metadata, tech_*, ai_*)
- [ ] Update SamplePreviewModal to show clip detail with structured tabs
- [ ] "Assign to Lead" section uses dataset_clips with lead_id
- [ ] Lead-specific clips show with appropriate badge
- [ ] Typecheck passes
- [ ] Verify in browser
- [ ] **Unit Tests:** Test lead-specific filtering
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `frontend-expert`, `code-reviewer`

---

### US-015: Update S3 annotation fetch API for clips

**Description:** As a developer, I need the portal's S3 annotation fetch endpoint to work with clips table data instead of dataset_samples.

**Acceptance Criteria:**
- [ ] Update `src/app/api/portal/s3-annotation/route.ts` to look up clip by ID from clips table
- [ ] **Switch to admin client** for clips lookup (service role bypasses RLS — session client would be blocked since clips has no per-user SELECT policy)
- [ ] Use clip's ann_annotation_key to fetch annotation-data.json from S3
- [ ] Use clip's s3_bucket for the correct bucket
- [ ] **Annotation caching**: current code caches to `dataset_samples.metadata_json` — update to cache into `clips.ann_metadata` instead. Background write uses admin client.
- [ ] Apply same scrubbing (scrubS3Urls, stripSensitiveFields) as before
- [ ] Typecheck passes
- [ ] **Unit Tests:** Test annotation fetch with valid and missing annotation keys, test caching writes to clips.ann_metadata
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `nextjs-expert`, `code-reviewer`

---

### US-016: Drop legacy tables and functions

**Description:** As a developer, I need to remove the old tables, RPC functions, and compatibility code once all surfaces are confirmed working on the new schema.

**Acceptance Criteria:**
- [ ] All admin, portal, MCP, and loader code confirmed working on staging with clips table
- [ ] Drop `dataset_samples` table
- [ ] Drop `video_index` table
- [ ] Drop `lead_custom_samples` table
- [ ] Drop `match_samples` RPC function
- [ ] Drop `match_video_index` RPC function
- [ ] Remove deprecated TypeScript types (DatasetSample, VideoIndexRecord, LeadCustomSample)
- [ ] Remove any remaining references to old table names in code
- [ ] Typecheck passes
- [ ] **Code Review:** Run code-reviewer agent to verify no remaining references

**Recommended agents/skills:** `code-reviewer`

---

### US-017: Replicate migration to production

**Description:** As a developer, I need to apply the validated staging migration to the production database.

**Reference docs:**
- Plan: `tasks/docs/unified-clip-architecture-plan.md` (sections: Phase 12)
- Requirements: `tasks/docs/unified-clip-architecture-requirements.md` (sections: Requirement 14)

**Acceptance Criteria:**
- [ ] All staging validation passes (search works, portal renders, MCP tools return data)
- [ ] Switch .env.local to prod Supabase credentials
- [ ] Apply DDL migration (clips table, dataset_clips, match_clips RPC, indexes)
- [ ] Run video_index → clips migration script (145k rows)
- [ ] Run dataset_samples → clips + dataset_clips migration script (714 rows, with re-embedding)
- [ ] Run caption rebuild script
- [ ] Validate row counts, search results, portal rendering on prod
- [ ] Deploy app code to Vercel (push to main)
- [ ] Drop legacy tables on prod after 24h soak period
- [ ] **Code Review:** Run code-reviewer agent on migration output

**Recommended agents/skills:** `python-backend-expert`, `code-reviewer`

---

### US-018: Seed data for UAT

**Description:** As QA, I need realistic test data seeded in staging so UAT can exercise all UI states.

**Acceptance Criteria:**
- [ ] Create `scripts/seed-uat-clips.ts` script
- [ ] Seed: 5 datasets across different categories, 50+ clips with varied metadata (some with all fields, some sparse)
- [ ] Seed: 3 test leads with dataset access and lead-specific clip assignments
- [ ] Seed: clips with different enrichment states (annotation only, AI only, both, neither)
- [ ] Seed: clips with different resolutions (720p, 1080p, 4K) and codecs
- [ ] Script is idempotent (safe to re-run)
- [ ] **Unit Tests:** Verify seed script creates expected counts
- [ ] **Code Review:** Run code-reviewer agent

**Recommended agents/skills:** `python-backend-expert`, `code-reviewer`

---

### US-019: Playwright E2E Test — Admin search and catalog management

**Description:** As QA, I need an end-to-end test verifying the unified admin search and catalog management workflow.

**Acceptance Criteria:**
- [ ] Create Playwright test file `tests/e2e/unified-clip-search.spec.ts`
- [ ] Test flow:
  1. Log into admin panel
  2. Navigate to catalog search
  3. Verify no mode toggle exists (single search)
  4. Search for "cooking kitchen"
  5. Verify results include clips from different sources with unified cards
  6. Filter by dataset "Egocentric Activity Capture"
  7. Verify filtered results show only matching clips
  8. Select 3 clips, click "Add to Dataset"
  9. Pick an existing dataset, confirm
  10. Navigate to that dataset's detail page
  11. Verify the 3 clips appear in the samples list
  12. Click a clip, verify detail modal shows structured tabs (Annotation | AI | Technical)
- [ ] Test includes assertions for all critical state transitions
- [ ] Test uses Page Object Model pattern
- [ ] Typecheck passes

**Recommended agents/skills:** Playwright MCP, `code-reviewer`

---

### US-020: Playwright E2E Test — Portal clip viewing

**Description:** As QA, I need an end-to-end test verifying leads can view clips with structured metadata in the portal.

**Acceptance Criteria:**
- [ ] Create Playwright test file `tests/e2e/portal-clip-view.spec.ts`
- [ ] Test flow:
  1. Log into portal as test lead (password auth)
  2. Navigate to catalog
  3. Verify dataset cards show with correct sample counts
  4. Click into a dataset
  5. Verify gallery shows clip thumbnails
  6. Click a clip
  7. Verify detail modal shows Annotation tab with category/activities
  8. Switch to Technical tab, verify resolution/fps/duration display
  9. Verify no internal fields visible (project_tag, ai_source, etc.)
- [ ] Test responsive at desktop and mobile viewports
- [ ] Typecheck passes

**Recommended agents/skills:** Playwright MCP, `code-reviewer`

---

### US-021: Playwright MCP User Acceptance Testing

**Description:** As QA, I need to perform user acceptance testing via Playwright MCP browser automation against the running dev server to verify the complete feature works end-to-end.

**Acceptance Criteria:**
- [ ] Seed the database using `scripts/seed-uat-clips.ts`
- [ ] Start local dev server on port 3020 pointed at staging DB
- [ ] Use Playwright MCP to navigate through admin search → find clips → add to catalog → view in portal
- [ ] Take screenshots at key checkpoints for visual verification
- [ ] Verify all UI states render correctly (loading, empty, populated, error)
- [ ] Test responsive behavior at desktop and mobile viewports
- [ ] Test MCP tools via curl: search_clips, create_custom_catalog, download_clips
- [ ] Verify admin preview mode works on portal pages
- [ ] Document any issues found and fix critical blockers

**Recommended agents/skills:** Playwright MCP, `webapp-testing` skill, `code-reviewer`

---

## Functional Requirements

- FR-1: Create `clips` table with structured columns per data source (ann_*, tech_*, ai_*)
- FR-2: Create `dataset_clips` join table (dataset_id, clip_id, lead_id)
- FR-3: Create `match_clips` RPC function for unified vector search
- FR-4: Migrate all video_index rows (145k) into clips with no data loss
- FR-5: Migrate all dataset_samples rows (714) into clips, re-embedding at 768 dims
- FR-6: Create dataset_clips rows preserving all dataset + lead relationships
- FR-7: Admin search uses single match_clips query (no mode toggle)
- FR-8: Portal dataset detail page queries clips via dataset_clips join
- FR-9: Clip detail view shows structured tabs (Annotation | AI Enrichment | Technical)
- FR-10: Portal annotation tab live-fetches annotation-data.json from S3
- FR-11: MCP search_clips replaces search_catalog + search_full_catalog
- FR-12: MCP create_custom_catalog inserts dataset_clips rows only (no data copy)
- FR-13: All loader scripts write to their own column groups on clips
- FR-14: Caption rebuild script derives caption_text + embedding from all source columns
- FR-15: All inserts use ON CONFLICT DO NOTHING to prevent bloat
- FR-16: Legacy tables (dataset_samples, video_index, lead_custom_samples) dropped after validation

## Non-Goals

- No changes to the datasets table structure (unchanged)
- No changes to lead_dataset_access table (unchanged)
- No changes to the landing page or case studies
- No new enrichment pipelines (just restructuring existing data)
- No change to the 768-dim embedding model (text-embedding-3-small)
- No real-time sync between prod and staging
- No automated enrichment triggers (rebuild is manual/scripted)

## Technical Considerations

- Supabase dashboard SQL editor has ~2 min timeout — use psql for long DDL/migrations
- IPv4 add-on is enabled on prod for direct psql access
- IVFFlat index builds need sufficient disk + maintenance_work_mem
- Prod disk was recently at 98% — VACUUM FULL reclaimed space, now at 3.8GB
- Use INSERT ON CONFLICT DO NOTHING throughout (not upsert) per project convention
- dataset_samples has 1536-dim embeddings that must be re-embedded at 768-dim during migration
- The `dataset_prefix_routes` table is preserved (used for scoping search to S3 prefixes)

## Success Metrics

- Zero data loss during migration (all clips, relationships, enrichments preserved)
- Admin search returns results from one unified index (no mode toggle)
- "Add clip to catalog" is < 100ms (single row insert vs multi-row copy)
- Portal renders clip detail with structured tabs (not raw JSON dump)
- MCP agents can search, curate, and manage clips through unified tools
- DB size reduced (no duplication between tables)

## Quality Assurance Requirements

Each user story must include:
1. **Unit Tests (Vitest)** — Test core logic, validation, and edge cases
2. **Code Review** — Run code-reviewer agent after EACH story completion to verify correctness, security, and patterns
3. **Type Safety** — All code must pass TypeScript strict mode

### Execution Guidelines
- **Parallelize work**: US-002 and US-003 can run in parallel (independent migration scripts). US-005 and US-010 can run in parallel (API and MCP). US-006, US-007, US-008 can run in parallel (different UI surfaces).
- **Code review every story**: After each user story is implemented, invoke the code-reviewer agent before marking it complete
- **Playwright MCP UAT**: US-021 is the final story for live browser-based user acceptance testing with seeded test data
- **Staging first**: US-001 through US-016 target staging. US-017 replicates to prod only after full staging validation.

## Canonical Schema (resolves cross-doc inconsistencies)

The following is the **single source of truth** for column names. All docs (design, requirements, plan) should align to this:

```sql
clips (
  -- Core
  id UUID PK DEFAULT gen_random_uuid(),
  s3_bucket TEXT NOT NULL,
  s3_key TEXT NOT NULL,
  mime_type TEXT,
  filename TEXT,
  UNIQUE(s3_bucket, s3_key),

  -- Annotation metadata (from S3 annotation-data.json)
  ann_metadata JSONB,              -- full generalData blob (varies by project type)
  ann_annotation_key TEXT,         -- relative S3 path to annotation-data.json (for live fetch)
  ann_specs_key TEXT,              -- relative S3 path to specs JSON

  -- Technical metadata (from annotation JSON media tracks or troveo metadata)
  tech_file_size_bytes BIGINT,
  tech_duration_seconds NUMERIC,
  tech_resolution_width INTEGER,
  tech_resolution_height INTEGER,
  tech_fps NUMERIC,
  tech_codec TEXT,
  tech_bit_depth INTEGER,

  -- AI enrichment (from Gemini, cobry, troveo captions)
  ai_caption TEXT,                 -- primary scene description for search
  ai_agent_context JSONB,          -- structured: {scene_summary, environments, activities, objects, camera_perspective}
  ai_enrichment_source TEXT,       -- 'gemini_frame', 'cobry_caption', 'troveo_metadata', etc.
  ai_enrichment_json JSONB,        -- raw enrichment blob (cobry multi-layer, troveo clip_annotations)

  -- Derived search fields (rebuilt by caption rebuild script)
  caption_text TEXT,               -- combined from all sources for embedding (NOT the same as ai_caption)
  embedding vector(768),           -- from caption_text via text-embedding-3-small
  caption_rebuilt_at TIMESTAMPTZ,  -- NULL = needs rebuild

  -- Timestamps
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
)

dataset_clips (
  id UUID PK DEFAULT gen_random_uuid(),
  dataset_id UUID FK → datasets NOT NULL,
  clip_id UUID FK → clips NOT NULL,
  lead_id UUID FK → leads,         -- NULL = visible to all, set = lead-specific
  added_by TEXT,
  note TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  -- NOTE: Can't use COALESCE in a UNIQUE constraint. Use expression index:
  -- CREATE UNIQUE INDEX idx_dataset_clips_unique
  --   ON dataset_clips (dataset_id, clip_id, COALESCE(lead_id, '00000000-0000-0000-0000-000000000000'))
)
```

## Codex Review Findings (resolved)

Issues caught by Codex review and how they're resolved:

1. **Schema column names inconsistent across docs** — Canonical schema above is the authority. Design/plan docs use different names but implementation follows the canonical schema.
2. **dataset_clips UNIQUE constraint blocks lead-specific rows** — Fixed: uses expression-based unique INDEX with `COALESCE(lead_id, sentinel_uuid)` (can't use COALESCE in a UNIQUE constraint per Postgres — must be an index).
3. **Design says ON CONFLICT DO UPDATE, PRD says DO NOTHING** — `DO NOTHING` is correct per bloat prevention rule. Design doc is overridden.
4. **Loaders writing caption_text directly vs source columns only** — Loaders write ONLY their own columns + set `caption_rebuilt_at = NULL`. Rebuild script derives `caption_text` + `embedding`. Plan is overridden by PRD.
5. **Live S3 fetch vs DB-stored ann_raw** — Hybrid approach: `ann_metadata` in DB for search/cards, `ann_annotation_key` stored so portal can live-fetch from S3 for detail modal. No `ann_raw` column.
6. **RLS strategy** — Clips table uses service role access (admin client), NOT per-user RLS. Matches existing portal pattern. Requirements doc is overridden.
7. **No rollback script** — Acceptable risk since staging-first validates everything. Old tables preserved until US-016 confirms all surfaces work.
8. **match_clips RPC return shape** — Must include: id, s3_bucket, s3_key, ai_caption, caption_text, similarity, ai_enrichment_source, ai_agent_context, mime_type, tech_resolution_width, tech_resolution_height, tech_fps, tech_duration_seconds, tech_codec, ann_metadata.
9. **COALESCE in UNIQUE constraint** — Postgres requires expression-based unique INDEX, not a UNIQUE constraint, when using COALESCE. Schema updated.
10. **Use HNSW not IVFFlat** — HNSW is Supabase-recommended default. Works on empty tables, handles incremental inserts, no re-tuning. Prod already uses HNSW.
11. **caption_text vs ai_caption** — `ai_caption` stores the AI-generated scene description. `caption_text` is the DERIVED combined field (annotation + tech + AI) used for embedding. Both needed.
12. **20 dataset_samples have NULL s3_object_key** — Migration must log and skip these. They likely have only media_url (external hosting).
13. **dataset_samples has no UNIQUE on S3 keys** — Multiple rows can point to same file. Migration must deduplicate: GROUP BY (s3_bucket, s3_object_key) → one clips row + multiple dataset_clips rows.
14. **631 of 714 dataset_samples lack embeddings** — US-003 re-embedding scope is larger than initially expected. Budget ~$0.50 for 631 embedding calls.
15. **GIN index on ann_metadata** — Add for `@>` containment queries on JSONB (category filtering).

## Open Questions

- Should `dataset_prefix_routes` be migrated to reference `clips` table or keep referencing `datasets`? (Currently references datasets — probably fine as-is since it maps S3 prefixes to datasets, not individual clips)
- Cobry/artlist/abaka clips have NO tech metadata. Accept NULL or backfill via ffprobe later? (Accept NULL for now — resolution filter shows "unknown" for these)
- annotation-platform `video_capture/` variant often has empty media attributes (no tracks). Accept NULL tech_* for these too? (Yes — same as cobry)

# Progress Log

Started: Fri Feb 27 18:52:31 EST 2026
Project: Admin Lead Management
Goal: Unknown

---

## US-001: Create lead API endpoint
**What I did:** Created POST /api/admin/leads endpoint that accepts { name, email, company, role, data_needs, use_case, admin_notes }, validates required fields (name, email) and email format via regex, inserts into Supabase leads table with status 'pending', returns 409 on duplicate email (error code 23505), and returns 201 with the created lead object. Uses the same cookie-based admin auth pattern (verifyAdminToken) as other admin routes.
**Files changed:** src/app/api/admin/leads/route.ts (new), prd.json (US-001 passes: true), progress.txt
**Learnings:** The leads table has a UNIQUE constraint on email, and Supabase surfaces PostgreSQL error code 23505 for unique violations. The existing admin routes all follow the same auth pattern: read admin-token cookie, verify via verifyAdminToken, return 401 if invalid. The insert data is built incrementally with optional fields only included if they are non-empty strings.

## US-004: Branded invite email template
**What I did:** Created src/lib/email/invite.ts with an exported sendInviteEmail({ to, name, magicLink }) function. Uses the Resend SDK with RESEND_API_KEY env var (returns { success: false, error: "Email not configured" } if missing). From address uses RESEND_FROM_EMAIL with fallback to team@claru.ai, formatted as "Claru AI <address>". Subject: "Your Claru Data Portal Access". HTML template uses the terminal aesthetic with dark background (#050505), accent green (#00ff88), JetBrains Mono font. Body includes personalized greeting, welcome message about portal access, prominent "Access Portal" button with the magic link, 24-hour expiry note, and support contact (team@claru.ai). Includes plain text fallback. Function never throws -- all errors are caught and returned as { success: false, error: string }.
**Files changed:** src/lib/email/invite.ts (new), prd.json (US-004 passes: true), progress.txt
**Learnings:** The existing email templates in src/lib/email.ts use slightly different colors (#0a0908 bg, #92B090 accent) than the landing page design system (#050505, #00ff88). The PRD explicitly requested the landing page colors for this template, creating a more terminal-aesthetic feel. The Resend SDK accepts a `text` property alongside `html` for plain text fallback. The function instantiates Resend inside the function body (after checking the API key) rather than at module scope to avoid errors when the env var is missing.

## US-002: Delete lead API endpoint with full cleanup
**What I did:** Added DELETE handler to src/app/api/admin/leads/[id]/route.ts alongside the existing GET and PATCH handlers. The handler follows the established auth pattern (cookie-based admin token verification), fetches the lead (404 if not found), then performs cleanup in the correct order: (1) deletes all rows from lead_dataset_access where lead_id matches, (2) if the lead has a supabase_user_id, calls supabase.auth.admin.deleteUser() with a console.warn on failure (user may already be gone), (3) deletes the lead row from the leads table. Returns 200 with { message: "Lead deleted" } on success, with proper error responses at each step.
**Files changed:** src/app/api/admin/leads/[id]/route.ts, prd.json (US-002 passes: true), progress.txt
**Learnings:** Deletion order is critical for referential integrity: child records (lead_dataset_access) must be deleted before the parent (leads). The auth user deletion is treated as best-effort since the user may have already been manually removed from Supabase Auth -- console.warn is used instead of console.error to signal this is a non-fatal condition. The supabase.auth.admin.deleteUser() pattern was confirmed from the approve route which uses supabase.auth.admin.createUser() and generateLink().

## US-005: Approve endpoint with send_invite option
**What I did:** Updated POST /api/admin/leads/[id]/approve to accept optional { send_invite: boolean, admin_notes?: string } in the request body. send_invite defaults to false for backward compatibility. After the lead is approved and the magic link is generated, if send_invite is true AND a magic link was successfully generated, the handler calls sendInviteEmail() from @/lib/email/invite with the lead's email, name, and magic link. The response now includes invite_sent (boolean) and optionally invite_error (string). If email sending fails, the approval still succeeds -- the lead status is "approved" but invite_sent is false with the error reason. If send_invite is true but the magic link generation failed, a specific error message is returned. When send_invite is false or omitted, existing behavior is completely unchanged with invite_sent: false in the response.
**Files changed:** src/app/api/admin/leads/[id]/approve/route.ts, prd.json (US-005 passes: true), progress.txt
**Learnings:** The magicLink variable was already being generated but never used -- this was the key integration point. The sendInviteEmail function from US-004 was designed to never throw, returning { success, error? } instead, which makes the error isolation straightforward: just check result.success and propagate result.error. The invite_error field is only included in the response when there is an actual error (using spread with conditional), keeping the response clean for the success case.

## US-003: Expand PATCH endpoint to accept all lead fields
**What I did:** Expanded the PATCH handler in src/app/api/admin/leads/[id]/route.ts to accept all editable lead fields: name, email, company, role, data_needs, use_case, admin_notes, and status. Added upfront validation: name must be a non-empty string if provided, email is validated with the same regex from the POST route (/^[^\s@]+@[^\s@]+\.[^\s@]+$/), and status is validated against the allowed values (pending, approved, rejected). Each field is only included in the Supabase update if provided as a string (omitted fields are not touched). String fields are trimmed before storage. Added email uniqueness conflict handling: if Supabase returns error code 23505, the endpoint returns 409 with "Email already in use by another lead". Existing admin_notes and status behavior is unchanged.
**Files changed:** src/app/api/admin/leads/[id]/route.ts, prd.json (US-003 passes: true), progress.txt
**Learnings:** The validation is structured in two phases: (1) upfront validation with early 400 returns for invalid values, and (2) building the updateFields object from valid string values. This separation keeps the validation logic clear and avoids partially building an update object before discovering an invalid field. The admin_notes field intentionally does not get trimmed (preserving the original behavior where notes are stored as-is), while all other string fields are trimmed to match the POST route's pattern. The email uniqueness conflict (23505) handling mirrors the POST route but with a different error message since it's an update context ("already in use by another lead" vs "already exists").

## US-006: Re-send invite API endpoint
**What I did:** Created POST /api/admin/leads/[id]/invite endpoint that re-sends magic link invites to already-approved leads. The handler verifies admin auth (cookie-based token), fetches the lead (404 if not found), returns 400 if the lead status is not "approved", handles the edge case where an approved lead has no supabase_user_id by creating the auth user first (same createUser + listUsers fallback pattern as the approve endpoint), generates a fresh magic link via supabase.auth.admin.generateLink with redirectTo pointing to /portal/auth/callback, and sends the invite email via sendInviteEmail. If the auth user was created as part of the edge case handling, the supabase_user_id is persisted back to the lead record. Returns { success: true } on success or { success: false, error: string } on failure.
**Files changed:** src/app/api/admin/leads/[id]/invite/route.ts (new), prd.json (US-006 passes: true), progress.txt
**Learnings:** Unlike the approve endpoint where invite failure is non-fatal (the approval still succeeds), the invite endpoint's sole purpose is sending the email, so failures at any stage (magic link generation, email sending) result in error responses. The edge case of a missing supabase_user_id on an approved lead requires both creating the auth user AND persisting the ID back to the lead record so future re-sends don't repeat the user creation. The _request parameter is prefixed with underscore since the body is not consumed (no configurable options for re-send).

## US-007: Create lead slide-over panel on leads list page
**What I did:** Added a `[+ new lead]` button in the filter/search bar area of AdminLeadsTable, positioned next to the search input. Clicking it opens a slide-over panel from the right edge with a create lead form. The panel follows the SampleEditPanel pattern: fixed position right-0, full height, 400px max width, dark background with accent border-left, backdrop overlay, close via X button or Escape key, slide-in animation (translateX(100%) to translateX(0)). The form includes Name (required), Email (required with format validation), Company, Role, Data Needs, Use Case, and Admin Notes fields. Client-side validation shows inline errors for required fields and email format. Submit POSTs to /api/admin/leads. On 201 success: closes panel, shows success alert, calls router.refresh(). On 409: shows "A lead with this email already exists" error. On other errors: shows the error message inline. The CreateLeadPanel is implemented as a sibling component within the same file, keeping the state management simple -- showCreatePanel boolean lives in AdminLeadsTable.
**Files changed:** src/app/admin/leads/AdminLeadsTable.tsx, prd.json (US-007 passes: true), progress.txt
**Learnings:** The SampleEditPanel pattern uses a `style jsx global` block for the slideIn keyframe animation -- this is the Next.js styled-jsx approach. Kept the same pattern but renamed the keyframe to slideInRight to avoid collisions. The panel state is managed entirely within AdminLeadsTable since it's already a client component -- no need to modify page.tsx or pass callbacks from the server component. The form resets naturally because the CreateLeadPanel component unmounts when showCreatePanel becomes false, destroying all local state.

## US-008: Edit mode toggle on lead detail page
**What I did:** Added isEditing boolean state, editForm state object (EditForm interface with name, email, company, role, data_needs, use_case), and isSavingEdit loading state to LeadDetailClient. Added [edit] button to the lead info card header that toggles into edit mode. In edit mode, name and email become text/email inputs in the header area, company and role become text inputs via EditField sub-component, and data_needs and use_case become textareas via EditTextarea sub-component. Non-editable fields (created_at, updated_at, supabase_user_id, status) remain as read-only display. Two action buttons in edit mode: [save] (accent green) and [cancel] (muted). Cancel reverts editForm to current lead values without API call. Save performs diff detection -- compares each editForm field against the current lead state and only includes changed fields in the PATCH request body. On 409, shows "Email already in use by another lead" error toast and stays in edit mode. On success, updates lead state from response, syncs editForm, exits edit mode, and shows success toast. Created two reusable sub-components: EditField (text input) and EditTextarea (textarea) with consistent design system styling. Admin notes section remains independently editable with its own save mechanism.
**Files changed:** src/app/admin/components/LeadDetailClient.tsx, prd.json (US-008 passes: true), progress.txt
**Learnings:** The diff detection pattern (comparing editForm values to lead values field-by-field and only sending changed fields) avoids unnecessary server-side validation for unchanged fields and prevents overwriting concurrent changes to other fields. The makeEditForm helper function is used in three places (initial state, edit toggle, cancel) to ensure consistent form initialization. Keeping the EditField and EditTextarea as separate sub-components outside the main component avoids re-mounting on every parent render and keeps the JSX in the main component clean.

## US-009: Delete lead button with confirmation dialog
**What I did:** Added a "Danger Zone" section at the bottom of LeadDetailClient with a [delete lead] button. Clicking it opens a fixed-position confirmation modal with a dark overlay (bg-black/60). The dialog warns about permanent removal of the lead, their portal access, and dataset grants. For approved leads, an additional warning about revoking portal login is shown. The dialog requires the user to type the lead's email exactly to enable the [confirm delete] button. On confirm, it calls DELETE /api/admin/leads/${lead.id}. On success, shows a success toast and redirects to /admin/leads via router.push. On error, shows an error toast and closes the dialog. The dialog is dismissible via [cancel] button or Escape key. Added three state variables: showDeleteDialog (boolean), deleteConfirmEmail (string), isDeleting (boolean). Imported useRouter from next/navigation for the redirect.
**Files changed:** src/app/admin/components/LeadDetailClient.tsx, prd.json (US-009 passes: true), progress.txt
**Learnings:** The email confirmation pattern is a well-established UX safety pattern (GitHub uses repository name, AWS uses resource name). Placing the Escape key listener in a useEffect with showDeleteDialog as a dependency ensures the listener is only active when the dialog is open and properly cleaned up when dismissed. The autoFocus attribute on the confirmation input improves usability by focusing the input immediately when the dialog appears. The delete handler resets the dialog state in the catch block (not finally) because on success the component will unmount due to the router.push redirect.

## US-010: Invite controls on lead detail page
**What I did:** Replaced the single Approve button with a dropdown menu showing two options: [approve & send invite] (sends POST with send_invite: true) and [approve only] (sends POST with send_invite: false). The dropdown uses a relative-positioned container with an absolute-positioned menu below the trigger button, styled with dark bg, border, z-10, and font-mono text-xs. Added click-outside and Escape key handlers to dismiss the dropdown. Updated handleApprove to accept a sendInvite boolean parameter and send { send_invite: sendInvite } in the request body. Toast messages now vary based on the response: "Lead approved and invite sent to {email}" when invite_sent is true, "Lead approved (no invite sent)" when neither invite_sent nor invite_error, and "Lead approved but invite failed: {error}" as a warning toast when invite_error is present. Added a "warning" type to the Toast interface with yellow/amber styling using var(--warning). For already-approved leads, added a [re-send invite] button that POSTs to /api/admin/leads/${lead.id}/invite, with loading state showing [sending...] and appropriate success/error toasts. Existing reject and reset-to-pending buttons are unchanged.
**Files changed:** src/app/admin/components/LeadDetailClient.tsx, prd.json (US-010 passes: true), progress.txt
**Learnings:** The dropdown pattern requires both a click-outside listener (mousedown on document) and an Escape key listener, both managed in a useEffect gated on the showApproveMenu boolean. Using a ref (approveMenuRef) on the container div allows the click-outside handler to correctly detect clicks inside the dropdown. The handleApprove function closes the menu before starting the async operation (setShowApproveMenu(false) first) to avoid the dropdown staying open during the loading state. The warning toast type extends the existing success/error pattern to provide a distinct visual for "succeeded with caveats" scenarios like approve-succeeded-but-invite-failed.
